[project]
name = "prediction-agent-backtest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "prediction-agent-shared",
    "prediction-agent-schemas",
    "prediction-agent-forecasting",
    "prediction-agent-calibration",
    "prediction-agent-execution",
    "pandas>=2.2",
    "numpy>=1.26",
]

[tool.uv.sources]
prediction-agent-shared = { workspace = true }
prediction-agent-schemas = { workspace = true }
prediction-agent-forecasting = { workspace = true }
prediction-agent-calibration = { workspace = true }
prediction-agent-execution = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/backtest"]
